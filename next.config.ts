import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:["files.edgestore.dev","res.cloudinary.com","placehold.coplacehold.co","placehold.co"]
  }
};

export default nextConfig;
